android {
    compileSdkVersion 30  // Updated from 28 to 30 for MABS 11
}

allprojects {
    repositories {
        google()  // Replaced maven.google.com with standard google() repo
        mavenCentral()
    }
}

dependencies {
    implementation 'com.google.android.play:app-update:2.1.0'  // Kept current version (still valid)
    implementation 'com.google.android.material:material:1.5.0'  // Replaced Support Design with Material Components
}
